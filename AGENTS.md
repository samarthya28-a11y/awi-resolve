<!-- token-governor -->
# Token budget (always on, keep short)

Prefer a new chat plus `.ai/HANDOFF.md` over a long thread. Search, then read only the slice you will change. Do not paste files, dumps, or architecture essays unless asked. Do not skip tests, types, or required reads to save tokens.

If `.ai/SESSION.md` or `.ai/HANDOFF.md` exists, read those before exploring the repo. After every finished request: replace HANDOFF (≤60) and SESSION (≤40), then reply. On “handoff”, “compact”, or “new chat”: write those files, then stop.

Details: `.ai/PROTOCOL.md` (or the Token Governor `PROTOCOL.md`).
