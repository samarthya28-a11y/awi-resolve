---
product: Gespage Client
aliases: gespage, gespage client, print client, epure client, cartadis client
version: FILL IN (e.g. 3.x)
---

# Deploying the Gespage Client

> ⚠️ SKELETON — REPLACE WITH THE OFFICIAL MANUAL. The structure below is a
> starting point. Paste in Alpha Web's / Cartadis's real Gespage client
> deployment procedure so the AI's guidance is exactly right. Do NOT include
> licence keys or passwords here — the customer enters those at install time.

## Prerequisites
- Windows 10/11.
- The Gespage **server address** for this customer (e.g. https://gespage.<customer>.local:<port>).
- Network access from the PC to that Gespage server.
- (FILL IN) Java runtime or any other dependency the client needs, if applicable.

## Where to get the installer
- (FILL IN) The approved Alpha Web download location for the Gespage client
  installer. Only use a trusted/signed source.

## Steps
1. (FILL IN) Download the Gespage client installer from the approved source.
2. (FILL IN) Run the installer.
3. (FILL IN) Enter the Gespage server address when prompted.
4. (FILL IN) Any post-install configuration (default printer, user auth mode, etc.).

## Required settings
- Gespage server address → (where it's entered)
- (FILL IN) Any other required settings.

## Verify it worked
- (FILL IN) e.g. the Gespage client icon appears in the system tray and shows
  "connected", a test print is tracked in the Gespage server, etc.

## Common problems
- Client can't reach the server → check the server address and that the PC can
  reach it (the AI can test this with its network check).
- (FILL IN) Other known Gespage client issues and fixes.
